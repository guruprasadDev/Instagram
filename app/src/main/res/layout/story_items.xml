<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="storyFeed"
            type="com.guruthedev.instagram.dataClass.InstaStatus" />

    </data>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="@dimen/hundred"
        android:orientation="vertical"
        android:padding="@dimen/marginSmall">

        <RelativeLayout
            android:id="@+id/profile_layout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_above="@+id/profile_name"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="@dimen/marginSmall">

            <ImageView
                android:id="@+id/profile_img"
                android:layout_width="@dimen/sixty"
                android:layout_height="@dimen/sixty"
                android:layout_centerInParent="true"
                android:src="@{storyFeed.picture}" />

            <ImageView
                android:id="@+id/ic_add_img"
                android:layout_width="@dimen/menu_margin"
                android:layout_height="@dimen/marginMediumBig"
                android:layout_alignParentEnd="true"
                android:layout_alignParentBottom="true"
                android:src="@drawable/add" />

        </RelativeLayout>

        <TextView
            android:id="@+id/profile_name"
            android:layout_width="@dimen/sevFive"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:ellipsize="end"
            android:gravity="center"
            android:singleLine="true"
            android:text="@{storyFeed.name}"
            android:textColor="@color/gray"
            android:textSize="@dimen/fourteen" />

    </RelativeLayout>
</layout>
